import {gsap,durM} from "../core/tokens.js";export function initAccordions(){document.querySelectorAll("[data-accordion]").forEach(acc=>{acc.querySelectorAll("[data-accordion-trigger]").forEach(btn=>{const panel=document.getElementById(btn.getAttribute("aria-controls"));if(!panel)return;btn.addEventListener("click",()=>{const open=btn.getAttribute("aria-expanded")==="true";btn.setAttribute("aria-expanded",String(!open));if(open)gsap.to(panel,{height:0,duration:durM,ease:"InOut"});else{panel.hidden=false;gsap.to(panel,{height:panel.scrollHeight,duration:durM,ease:"InOut",onComplete:()=>panel.style.height="auto"})}})})})}