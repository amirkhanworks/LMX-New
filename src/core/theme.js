import {gsap,ScrollTrigger,durS} from "./tokens.js";export function initTheme(){const fixed=document.querySelectorAll("[data-theme]");document.querySelectorAll("main [data-bg]").forEach(sec=>ScrollTrigger.create({trigger:sec,start:"top 50%",end:"bottom 50%",onToggle:self=>{if(!self.isActive)return;const bg=sec.dataset.bg;fixed.forEach(el=>el.classList.remove("theme_on-light","theme_on-dark","theme_on-color"));fixed.forEach(el=>el.classList.add(`theme_on-${bg}`));const root=document.documentElement;root.style.setProperty("--ui-fg",bg==="light"?"var(--ink)":"var(--paper)");root.style.setProperty("--ui-muted",bg==="light"?"var(--ink-30)":"var(--paper-30)");root.style.setProperty("--ui-thumb",bg==="light"?"var(--ink)":"var(--amber)");}}));}