import Lenis from "lenis";import {gsap,ScrollTrigger} from "./tokens.js";export let lenis=null;export function initLenis(reduced){if(reduced)return null;lenis=new Lenis({duration:1.2,smoothWheel:true,touchMultiplier:2,easing:t=>Math.min(1,1.001-Math.pow(2,-10*t))});lenis.on("scroll",ScrollTrigger.update);gsap.ticker.add(t=>lenis.raf(t*1000));gsap.ticker.lagSmoothing(0);return lenis;}