import {gsap,MQ} from "../core/tokens.js";export function initControlsZoom({reduced}){const area=document.querySelector("[data-zoom]");if(!area||reduced)return;const under=area.querySelector(".zoom__under"),grid=area.querySelector("[data-zoom-grid]");gsap.matchMedia().add(MQ.desk,()=>{const tl=gsap.timeline({defaults:{ease:"none"},scrollTrigger:{trigger:area,start:"top top",end:"bottom bottom",scrub:true,invalidateOnRefresh:true}});if(under)tl.to(under,{autoAlpha:.08,duration:.35},0);if(grid)tl.to(grid,{y:-18,scale:1.03,duration:1},"<");});}